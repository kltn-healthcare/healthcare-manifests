# Service for shared Redis cache
apiVersion: v1
kind: Service
metadata:
  name: healthcare-redis
  namespace: healthcare-db
spec:
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  selector:
    app: healthcare-redis