apiVersion: apps/v1
kind: Deployment
metadata:
  name: healthcare-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: healthcare-admin
  template:
    metadata:
      labels:
        app: healthcare-admin
    spec:
      containers:
        - name: healthcare-admin
          image: healthcare-admin-image:latest
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          ports:
            - containerPort: 3002
          envFrom:
            - secretRef:
                name: healthcare-app-secret
      imagePullSecrets:
        - name: gitea-registry-secret
