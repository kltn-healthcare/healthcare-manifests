# Headless Service for stable network identity
apiVersion: v1
kind: Service
metadata:
  name: auth-db
  namespace: healthcare
spec:
  clusterIP: None
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    app: auth-db