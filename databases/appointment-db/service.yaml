# Headless Service for stable network identity
apiVersion: v1
kind: Service
metadata:
  name: appointment-db
  namespace: healthcare
spec:
  clusterIP: None
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    app: appointment-db