# Headless Service for stable network identity
apiVersion: v1
kind: Service
metadata:
  name: medical-record-db
  namespace: healthcare
spec:
  clusterIP: None
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    app: medical-record-db